Design note:
Free drop and shock machine both map to impact_shock in v1 taxonomy.
Boundary condition differences are represented via constraint_state.
Consider introducing test_method or free_drop driver in v2 if needed.